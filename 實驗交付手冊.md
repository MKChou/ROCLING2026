# 實驗交付手冊：你要做什麼、要交回什麼

> 目的：你按本手冊執行實驗，把結果以指定格式交回，我即可將數據、圖表與分析文字補進 `rocling2026_draft.tex`。
> 論文對應：表 2（主結果）、表 3（幻覺）、圖 1（trade-off）、5.1–5.5 節敘述。
> 原則：**每個實驗都輸出一份 CSV ＋ 原始輸出 JSON/文字檔**，命名照本手冊，交回時整包 zip 或直接放進 `720\results\` 資料夾即可。

---

## 總覽：五個實驗

| 實驗 | 名稱 | 對應論文位置 | 優先度 |
|------|------|--------------|--------|
| E1 | 準確度基準（國語/台語 CER＋關鍵詞） | 表 2、5.1 節 | ★★★ 必做 |
| E2 | 延遲與資源（RTF/延遲/記憶體） | 表 2、圖 1、5.1 節 | ★★★ 必做 |
| E3 | 幻覺診斷測試 | 表 3、5.2 節 | ★★★ 必做（本文賣點） |
| E4 | VAD 門控緩解 | 5.3 節 | ★★ 強烈建議 |
| E5 | 錯例收集（質性分析） | 5.4 節 | ★★ 半自動，順手做 |

五個部署方案代號（與論文一致）：
- **D1** = Whisper large-v3（GPU）
- **D2** = Whisper small（本地）
- **D3** = D2 + Silero VAD 門控
- **D4** = Kaldi（實驗室既有系統）
- **D5** = Nemotron 3.5 ASR 0.6B + OpenCC s2twp

---

## 事前準備（一次性）

### 工具安裝

```bash
# Python 3.10+ 環境
pip install faster-whisper          # D1/D2/D3 (或用實驗室現有 whisper 方案)
pip install silero-vad              # D3
pip install opencc-python-reimplemented  # D5 後處理
pip install jiwer                   # CER 計算
pip install soundfile librosa       # 音檔處理
# D5: NeMo（建議獨立 conda env，依 HuggingFace model card 指示）
pip install "nemo_toolkit[asr]"
# D4: 實驗室既有 Kaldi，不用重裝
```

### 測試資料準備

| 資料 | 來源 | 你要做的事 |
|------|------|-----------|
| 國語測試集 | Common Voice zh-TW test | 下載後抽樣 500–1000 句（固定 random seed），統一轉 16kHz wav |
| 台語測試集 | 實驗室既有（TAT 或自有） | 挑 200–500 句，附標準答案文字檔 |
| ACP 關鍵詞句 | 自建 | 見 E1 附錄 A，30–50 句，找 2–4 人錄音（國語為主，行有餘力加台語版） |
| 幻覺診斷集 | 自建 | 見 E3，約 100 段 |

**音檔格式統一**：16kHz、單聲道、wav。每個測試集一個資料夾＋一個 `manifest.csv`：

```csv
audio_path,reference_text,language
cv_tw/clip_0001.wav,今天天氣很好,zh-tw
```

---

## E1：準確度基準

### 目的
填論文**表 2** 的「國語 CER / 台語 CER / 關鍵詞錯誤率」三欄。

### 步驟
1. 五個系統（D1、D2、D4、D5；D3 在 E4 做）各跑三個測試集：國語、台語、ACP 關鍵詞句。
2. D5 輸出先存「原始簡體」，再過 OpenCC s2twp 存「轉繁後」——**兩版都要留**（簡繁錯誤分析用）。
3. D5 跑台語直接標 N/A，不用跑（或跑一次留紀錄，證明輸出是無意義的，可當錯例）。
4. 用 jiwer 算 CER。計算前先做正規化：全形轉半形、移除標點、繁體統一（建議我寫好一支 `score.py` 給你，見文末「我可以先幫你的」）。
5. 關鍵詞錯誤率：對 ACP 句子，檢查每個關鍵詞（附錄 A 詞表）是否被正確辨識，錯誤率 = 辨識錯的關鍵詞數 / 總關鍵詞數。

### 交回格式

`results/E1_accuracy.csv`：

```csv
profile,testset,n_utts,cer,keyword_total,keyword_err,keyword_err_rate
D1,mandarin,1000,4.2,120,5,4.2
D1,taiwanese,300,8.5,,,
D5,mandarin_raw_simplified,1000,6.1,,,
D5,mandarin_opencc,1000,6.8,120,18,15.0
```

外加每個系統的**逐句輸出**（`results/E1_outputs/D1_mandarin.jsonl`，每行 `{"audio":"...","ref":"...","hyp":"..."}`）——我需要逐句輸出做錯例分析（E5）。

---

## E2：延遲與資源

### 目的
填**表 2** 的「RTF / 延遲 / 記憶體」欄，以及**圖 1** trade-off 圖。

### 步驟
1. 從國語測試集固定抽 50 句（同一批給所有系統）。
2. 每個系統在**同一台本地機器**（CPU）跑一輪、有 GPU 的（D1）在伺服器跑一輪。
3. 量測：
   - **RTF** = 總解碼時間 ÷ 音訊總長度（跑 3 次取平均，第 1 次 warm-up 不計）
   - **端到端延遲** = 單句從送入到完整輸出的時間（取 50 句平均與 P95）
   - **串流系統（D4/D5）另量首字延遲**（第一個字出現的時間），沒辦法量就註明
   - **峰值記憶體**：Linux 用 `/usr/bin/time -v`（看 Maximum resident set size），Windows 用工作管理員紀錄或 `psutil`
   - **模型大小**：檢查點檔案 GB 數
4. **記下硬體規格**（CPU 型號/核心數/RAM、GPU 型號/VRAM）——論文 4.3 節要寫。

### 交回格式

`results/E2_performance.csv`：

```csv
profile,device,rtf,latency_avg_s,latency_p95_s,first_token_s,peak_mem_gb,model_size_gb
D1,gpu_a100,0.05,1.2,2.0,,10.2,3.1
D2,cpu_i7,0.45,3.5,5.1,,2.1,0.5
D5,cpu_i7,0.08,0.3,0.5,0.08,1.2,0.6
```

外加一行文字：硬體規格（例：`CPU: Intel i7-12700, 32GB RAM; GPU: RTX 4090 24GB`）。

---

## E3：幻覺診斷測試（本文賣點，務必做完整）

### 目的
填**表 3**、寫 5.2 節。驗證「Kaldi 不易幻覺、Whisper 會、Nemotron 未知」。

### 診斷集製作（半天內可完成）

| 條件 | 段數 | 怎麼做 |
|------|------|--------|
| C1 純靜音 | 20 | 用 sox/程式產生 3、5、8、10 秒靜音各 5 段（可加極低底噪 -60dB 更真實） |
| C2 環境噪音 | 20 | 手機錄：診間/辦公室背景、電扇、街道、鍵盤聲，每段 5–10 秒，**確認無人聲** |
| C3 極短語音 | 20 | 找 2 人錄「好」「不要」「嗯好」「是」等 1–3 字，每段前後留 1–2 秒靜音 |
| C4 猶豫音 | 20 | 錄「嗯……」「啊……」「呃……」拖長 2–5 秒，不含實詞 |
| C5 正常 ACP 句 | 20 | 從 E1 的 ACP 關鍵詞句挑 20 句當對照組 |

`manifest` 多一欄 `condition`，C3/C4/C5 附參考文字，C1/C2 參考文字為空。

### 步驟
1. 五系統（含 D3）全部跑完 100 段。
2. 對每段記錄：輸出文字、輸出字數。
3. 判定指標：
   - **幻覺率**（C1、C2）：輸出非空（去除純標點/空白後仍有字）的段數比例
   - **平均插入字數**（C3、C4）：`max(0, 輸出字數 - 參考字數)` 的平均
   - **關鍵詞幻覺**（全部條件）：輸出中出現附錄 A 詞表中任一關鍵詞、但參考文字沒有的次數

### 交回格式

`results/E3_hallucination.csv`（逐段原始紀錄，我來彙總）：

```csv
profile,condition,audio_path,ref_text,hyp_text,hyp_len,is_nonempty,keyword_hallucinated
D2,C1,halluc/silence_01.wav,,謝謝大家收看,6,1,0
D4,C1,halluc/silence_01.wav,,,0,0,0
```

**特別注意**：C1/C2 條件下 Whisper 輸出的完整句子**原文照留**，論文 5.4 節要引用實際幻覺例句（如「謝謝大家收看」這類 YouTube 訓練殘留是經典錯例，很有說服力）。

---

## E4：VAD 門控緩解（D3）

### 目的
寫 5.3 節：VAD 能把 Whisper 幻覺率降多少？代價是什麼？

### 步驟
1. 用 Silero VAD（預設參數，threshold=0.5）在 D2 前面加一層：VAD 判定無語音 → 直接輸出空字串；有語音 → 只把語音段送 Whisper。
2. D3 重跑：(a) E3 全部 100 段（重點）、(b) E1 國語測試集（看 CER 是否因誤切上升）、(c) E2 的 50 句延遲量測（看額外開銷）。
3. 若時間夠，threshold 試 0.3/0.5/0.7 三檔，看幻覺率 vs CER 的變化（加分，非必要）。

### 交回格式
與 E1/E2/E3 相同格式，profile 填 `D3`（多檔 threshold 就填 `D3_t0.3` 等）。

---

## E5：錯例收集（順手做）

### 目的
5.4 節質性分析。不用額外跑實驗，從 E1/E3 的逐句輸出中挑。

### 你要挑的四類錯例（各 2–3 個）
1. **Whisper 靜音/噪音幻覺**：C1/C2 輸出的完整句子（E3 自然產出）
2. **D5 簡繁轉換殘留錯誤**：對照「原始簡體 vs OpenCC 轉繁 vs 正確答案」，找一簡對多繁錯例（發/髮、後/后、裡/里、麵/面、鬱/郁…）
3. **台語句子各系統輸出對照**：挑 2–3 句，列 D1/D2/D4 的輸出（D5 的無意義輸出也可列，顯示語言覆蓋缺口）
4. **極短語音/猶豫音的過度生成**：C3/C4 中插入字數最多的例子

### 交回格式
不拘，最簡單就是一個 `results/E5_error_examples.md`，每個錯例寫：音檔、參考答案、各系統輸出。

---

## 交回檢查清單

```
results/
├── E1_accuracy.csv
├── E1_outputs/          # 逐句 jsonl（每系統×每測試集一檔）
├── E2_performance.csv
├── E2_hardware.txt      # 硬體規格一行
├── E3_hallucination.csv
├── E4_*（併入 E1/E2/E3 格式，profile=D3）
└── E5_error_examples.md
```

另外請順手告訴我（論文 4 節要寫的設定）：
- [ ] Whisper 實作與版本（faster-whisper？openai/whisper？繁中/台語用的是哪個微調模型——匿名描述即可）
- [ ] Kaldi 系統的聲學模型架構（TDNN chain？）、語言模型與詞典大小（大概就好）
- [ ] Common Voice zh-TW 版本號與抽樣句數
- [ ] 台語測試集描述（來源、句數、標註方式）
- [ ] ACP 句錄音者資訊（幾位、年齡層、語言）——不涉及個資，寫「N 位成年語者」即可

---

## 附錄 A：ACP 關鍵詞表（初版，可請羅醫師增修）

**文件與制度類**：預立醫療決定、預立醫療照護諮商、病人自主權利法、醫療委任代理人、意願書、簽署
**處置類**：心肺復甦術、CPR、DNR、插管、氣切、洗腎、鼻胃管、葉克膜、輸血、抗生素、維生醫療
**照護類**：安寧緩和、緩和醫療、善終、末期、臨終、舒適照護、居家安寧
**對話類**：急救、放棄、拒絕、同意、家屬、討論、意願

### 附錄 B：ACP 測試句範例（自建 30–50 句用，先給 10 句起頭）

1. 我想要簽預立醫療決定。
2. 如果到了末期，我不要插管急救。
3. 心肺復甦術是什麼意思？
4. 我想跟家人討論安寧緩和醫療。
5. 醫療委任代理人可以幫我做什麼決定？
6. 我還在考慮要不要簽 DNR。
7. 洗腎跟鼻胃管我都不想要。
8. 預立醫療照護諮商要去哪裡預約？
9. 我希望最後能在家裡善終。
10. 這份意願書簽了以後可以改嗎？

（請按此風格擴充到 30–50 句；台語版由實驗室既有羅馬字/漢字規範處理。）

---

## 我可以先幫你的（開口即可）

1. **寫好全套跑分腳本**：`run_whisper.py`、`run_nemotron.py`、`score.py`（CER＋關鍵詞＋幻覺判定，含文字正規化）、`make_silence.py`（產生 C1 靜音檔）——你只要放音檔、跑指令。
2. **產生 ACP 測試句完整 50 句**供錄音用。
3. **實驗數據交回後**：我負責彙總、畫圖 1（matplotlib 出 PDF）、把表 2/表 3 填進 `.tex`、寫完 5.1–5.5 與摘要/結論的待補句、跑一次編譯檢查。

> 建議順序：先跑 E1 的 D1/D2/D5 國語（一天內可完成，論文就立住了）→ E3 幻覺（賣點）→ E2 → E4 → 台語與 E5。
