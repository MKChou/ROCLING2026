# 實驗室語料路徑（依 G: USB 腳本與文件整理）

> **主機**：`140.116.245.147`（SSH **`-p 24680`**，FRP 轉發；不是 122）  
> **本機存放**：`E:\data\22k_corpus`（2026-07-06：`trandition_zh`、`tw_clean`、`tw/corpus` 已解壓；台語文字在 `tw/corpus/148_kaldi_tw_corpus_300_corpus`）  
> **工作目錄**：`~/Linux_DATA/synthesis/corpus/corpus_manager/22k_corpus`

## 最可能的語料根目錄

```
~/Linux_DATA/synthesis/corpus/corpus_manager/22k_corpus/
```

（USB 腳本裡原路徑為 `/home/p76131482/Linux_DATA/...`，同一台機器不同使用者 home，結構應相同。）

## ROCLING 論文會用到的子目錄

### 台語測試集（E1 taiwanese）

**重要：`tw_clean` 只有 wav（無文字）；要算 CER 請用 `tw/`（含文字檔）。**

```
22k_corpus/tw/                              # 有 wav + 文字（整包約 212 GB）
22k_corpus/tw/corpus/148_kaldi_tw_corpus_300_corpus/
  └── kaldi_tw_F12/          # 例：S24_F12_041.wav + 同目錄文字
22k_corpus/tw_clean/                        # 僅 wav + .spec.pt（已下載，無參考句）
22k_corpus_processed/tw/corpus/...   # 前處理版（82k+ 句，報告 2026-02-07）
```

- 音檔命名：`S{speaker}_F{session}_{utt}.wav` 或 `S{speaker}_M{session}_{utt}.wav`
- `metadata.csv`（根目錄）為**羅馬拼音**，與 `tw_clean` wav **ID 不對應**，不宜當漢字 CER 參考

### 繁中／國語測試集（E1 mandarin）

```
22k_corpus/trandition_zh/
  ├── recognition_dataset/
  ├── ir01/22khz_male_trad/
  ├── azure_synthesis_with_vad/
  └── podcast/
```

優先看 `recognition_dataset` 或 `ir01/22khz_male_trad` 是否有 wav + 繁體句。

### 不建議當 zh-TW 國語集

```
22k_corpus/zh/corpus/opendataset/MAGIC/   # 簡體普通話 MAGICDATA
```

## 其他相關目錄（147 `Linux_DATA`）

| 目錄 | 用途 |
|------|------|
| `whisper_test` | Whisper 測試 |
| `hakka_data` | 客語 |
| `synthesis_api` / `syn_web` | TTS 合成 |
| `indivi_backup_155.sh` | 155 備份腳本 |

## ASR API（與語料分開）

- **149**：`http://140.116.245.149:5002/proxy` → D2 Whisper
- **147**：語料與合成；**155**：可能是備份／鏡像

## 下載範例

```powershell
# 台語含文字（建議先 SSH 看大小）
ssh -p 24680 an4096750@140.116.245.147 "du -sh ~/Linux_DATA/synthesis/corpus/corpus_manager/22k_corpus/tw ~/Linux_DATA/synthesis/corpus/corpus_manager/22k_corpus/tw/corpus"

# 方式 A：只下 tw/corpus（通常比整包 tw 小）
powershell -ExecutionPolicy Bypass -File scripts\env\download_corpus_scp.ps1 -Subset tw-corpus -Dest "E:\data\22k_corpus"

# 方式 B：整包 tw（約 200+ GB）
powershell -ExecutionPolicy Bypass -File scripts\env\download_corpus_scp.ps1 -Subset tw -Dest "E:\data\22k_corpus"

# 方式 C：伺服器先打包再 scp（與 rocling.tgz 相同做法）
# 在 147 上：tar czf ~/tw_corpus.tgz -C ~/Linux_DATA/synthesis/corpus/corpus_manager/22k_corpus/tw corpus
# 在本機：scp -P 24680 an4096750@140.116.245.147:~/tw_corpus.tgz E:\data\
```

```bash
# 在「你的電腦」上（已能 ssh 147）
scp -P 24680 -r an4096750@140.116.245.147:~/Linux_DATA/synthesis/corpus/corpus_manager/22k_corpus/tw/corpus E:\data\22k_corpus\tw\
```

## 仍缺、語料庫補不了

- 幻覺 C2（噪音）、C3（短語）、C4（遲疑音）— 需自建
- ACP 50 句 — 已有
